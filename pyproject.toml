[project]
name = "osimpy"
version = "0.1.0"
description = "Pythonic wrapper and tools for working with OpenSim models and simulations"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic-settings>=2.0",
    "polars",
    "opensim>=4.6",
]

[project.optional-dependencies]
dev = [
    "marimo",
    "pytest>=8",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
